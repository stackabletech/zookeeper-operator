---
# Source: zookeeper-operator/templates/deployment.yaml
# =============
# This file is automatically generated from the templates in stackabletech/operator-templating
# DON'T MANUALLY EDIT THIS FILE
# =============
apiVersion: apps/v1
kind: Deployment
metadata:
  name: RELEASE-NAME-deployment
  labels:
    app.kubernetes.io/name: zookeeper-operator
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "0.6.0-nightly"
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: zookeeper-operator
      app.kubernetes.io/instance: RELEASE-NAME
  template:
    metadata:
      labels:
        app.kubernetes.io/name: zookeeper-operator
        app.kubernetes.io/instance: RELEASE-NAME
    spec:
      serviceAccountName: RELEASE-NAME-serviceaccount
      securityContext: {}
      containers:
        - name: zookeeper-operator
          securityContext: {}
          image: "docker.stackable.tech/stackable/zookeeper-operator:0.6.0-nightly"
          imagePullPolicy: IfNotPresent
          resources: {}
          volumeMounts:
            - mountPath: /etc/stackable/zookeeper-operator/config-spec
              name: config-spec
      volumes:
        - name: config-spec
          configMap:
            name: RELEASE-NAME-configmap
